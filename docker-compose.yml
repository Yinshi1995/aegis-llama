services:
  aegis:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    environment:
      - PORT=7860
    volumes:
      - ollama_data:/root/.ollama
      - ./knowledge_base:/app/knowledge_base
      - ./scripts:/app/scripts

volumes:
  ollama_data:
